console.log("Exercise No 12");

console.log(
  `Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s name, print a message to them. The text of each message should be the same, but each message should be personalized with the person’s name.`
);
{
  let friendNames: string[] = ["Shehraz", "Hassan", "Ramzan"];
  let message: string =
    "how are you, are you free if yes then text me we can plan a study seesion for today.";
  console.log(friendNames[0] + " " + message);
  console.log(friendNames[1] + " " + message);
  console.log(friendNames[2] + " " + message);
}
